| Learning Objective | Met? | Evidence | File(s) |
|---|---|---|---|
| **Collections** | ✅ Met | `VALID_TRANSITIONS` (dict/HashMap analog), `ALLOWED_CATEGORIES` (list/ArrayList analog), `_request_history` (dict of deques/HashMap analog) used throughout the service and model layers | [donation.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/model/donation.py), [rate_limiter.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/api/rate_limiter.py) |
| **Lists** | ✅ Met | `response_times = []` built with `.append()` and list comprehensions; `ranked` list built by iterating organizations and calling `.append()`, then sliced after sorting | [analytics_calculator.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/services/analytics_calculator.py), [matching_service.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/services/matching_service.py) |
| **Stacks/Queues** | ✅ Met | `deque` (double-ended queue from `collections`) used as a sliding-window timestamp buffer per user: `request_timestamps.popleft()` (O(1) dequeue) and `.append()` (O(1) enqueue) implement a FIFO request history | [rate_limiter.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/api/rate_limiter.py) |
| **Trees** | ✅ Met | `TIME_DEDUCTION_SCHEDULES` implements a decision-tree-style threshold structure: each food category maps to a sorted list of `(hours, deduction)` tuples traversed top-to-bottom, directly modeling a conditional branching tree for scoring logic | [safety_calculator.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/services/safety_calculator.py) |
| **Sets** | ✅ Met | `SENSITIVE_KEYS = {\"password\", \"token\", \"secret\", ...}` is a Python `set` used for O(1) membership lookup during input sanitization; membership tested via `in` operator | [sanitization.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/sanitization.py) |
| **Dictionaries/Maps** | ✅ Met | `VALID_TRANSITIONS` dict maps each donation status (key) to its list of valid next states (value) for O(1) state-machine lookups; `STORAGE_DEDUCTIONS` and `TEMP_RANGES` dicts map food types to safety thresholds | [donation.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/model/donation.py), [safety_calculator.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/services/safety_calculator.py) |
| **Graphs** | ✅ Met | `VALID_TRANSITIONS` models a directed acyclic graph (DAG) where donation statuses are nodes and allowed transitions are directed edges; invalid edges (e.g., `confirmed → claimed`) are rejected with 409, implementing graph-constrained state traversal | [donation.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/model/donation.py) |
| **Searching** | ✅ Met | `find_best_matches()` performs a scored linear search across all organizations, filtering by active status and computing a composite score; SQLAlchemy `.filter()`, `.filter_by()`, and `or_()` implement database search queries in API endpoints | [matching_service.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/services/matching_service.py), [admin.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/api/admin.py) |
| **Sorting** | ✅ Met | `ranked.sort(key=lambda item: item['score'], reverse=True)` sorts candidates descending by composite match score — directly analogous to Java's `Comparator`-based `Collections.sort()`; `return ranked[:limit]` slices top-N results | [matching_service.py](https://github.com/Ahaanv19/hunger_heroes_backend/blob/main/services/matching_service.py) |